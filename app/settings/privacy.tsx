import { View, Text } from 'react-native';

export default function PrivacyScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Privacidade (Em breve)</Text>
        </View>
    );
}
