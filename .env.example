# ================================
# Kourt App Environment Variables
# ================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
#
# For EAS builds, set these as EAS Secrets:
# eas secret:create --name VARIABLE_NAME --value "your-value" --scope project
# ================================

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Mapbox Configuration
# Public token (safe to expose in client)
EXPO_PUBLIC_MAPBOX_TOKEN=pk.your-mapbox-public-token
# Download token (SECRET - only for builds)
RNMAPBOX_MAPS_DOWNLOAD_TOKEN=sk.your-mapbox-secret-token

# Stripe Configuration
# Publishable key (safe to expose)
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-key
# Secret key should ONLY be in Supabase Edge Functions, never in client

# API Configuration
EXPO_PUBLIC_API_URL=https://api.kourt.app

# Google OAuth (from Google Cloud Console)
EXPO_PUBLIC_GOOGLE_CLIENT_ID_WEB=your-google-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID_IOS=your-google-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID_ANDROID=your-google-client-id.apps.googleusercontent.com

# Sentry (optional)
SENTRY_DSN=https://your-sentry-dsn
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# ================================
# EAS SECRETS SETUP COMMANDS
# ================================
# Run these commands to set secrets for EAS builds:
#
# eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value "https://..."
# eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "eyJ..."
# eas secret:create --scope project --name EXPO_PUBLIC_MAPBOX_TOKEN --value "pk.eyJ..."
# eas secret:create --scope project --name RNMAPBOX_MAPS_DOWNLOAD_TOKEN --value "sk.eyJ..."
# eas secret:create --scope project --name EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY --value "pk_..."
#
# List all secrets:
# eas secret:list
# ================================
