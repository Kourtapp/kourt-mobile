import { View, Text } from 'react-native';

export default function ReferralScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Indicar Amigos (Em breve)</Text>
        </View>
    );
}
