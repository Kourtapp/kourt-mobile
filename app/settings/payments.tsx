import { View, Text } from 'react-native';

export default function PaymentsScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Pagamentos (Em breve)</Text>
        </View>
    );
}
