import { View, Text } from 'react-native';

export default function SecurityScreen() {
    return (
        <View className="flex-1 items-center justify-center bg-white">
            <Text className="text-xl font-bold">Segurança (Em breve)</Text>
        </View>
    );
}
